import { injectable } from "inversify";
import { IDataAccessController } from "../interfaces";

@injectable()
export class DataAccessController implements IDataAccessController {
    run(): void {

    }
}
